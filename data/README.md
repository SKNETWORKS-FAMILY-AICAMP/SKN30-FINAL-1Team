# 프로젝트 샘플 데이터

`sample/`과 `sanitized_docs/`에는 프로젝트 개발·데모용으로 변형된 데이터만 둡니다.
원본 데이터는 `raw/`에만 보관하며 Git에 포함하지 않습니다.

- `sample/`: 변형된 표 형식 샘플 데이터
- `sanitized_docs/`: 변형된 계약서·발주서 등 문서 샘플
- `raw/`: 원본 보관용 경로 (Git 제외)

변형본은 문서 구조와 업무 흐름을 유지하되, 개인·거래처 식별정보와 실제 모델·금액 등을 가상 값으로 변경한 자료입니다.
